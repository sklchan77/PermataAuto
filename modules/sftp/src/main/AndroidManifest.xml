<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    split="sftp"
    android:isFeatureSplit="true">

    <dist:module
        dist:instant="false"
        dist:title="@string/module_name_sftp">
        <dist:delivery>
            <!-- Unconditional install for ALL devices (Phones, Cars, TVs) -->
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>
</manifest>